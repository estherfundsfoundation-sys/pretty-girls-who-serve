{
  "name": "pretty-girls-who-serve",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs tests/portal/*.test.mjs"
  },
  "dependencies": {
    "stripe": "^18.5.0"
  }
}
